---
- name: set default keypair
  shell: solana config set --keypair {{ secrets_path }}/neonevm-keypair.json
  become: yes
  become_user: "{{ neonevm_user }}"
  environment:
    PATH: "{{ env_path }}"
  tags:
    validator.manage.config
